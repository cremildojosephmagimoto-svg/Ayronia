---
import Layout from '@layouts/Layout.astro';

const services = [
  {
    title: 'Eventos Corporativos',
    description: 'Serviços de catering para conferências, reuniões e eventos empresariais',
    icon: '🏢',
    features: ['Coffee Breaks', 'Almoços Executivos', 'Cocktails', 'Buffets Personalizados']
  },
  {
    title: 'Festas Privadas',
    description: 'Catering completo para aniversários, casamentos e celebrações familiares',
    icon: '🎉',
    features: ['Casamentos', 'Aniversários', 'Batizados', 'Festas Temáticas']
  },
  {
    title: 'Refeições Diárias',
    description: 'Serviço de entrega de refeições para empresas e residências',
    icon: '🍱',
    features: ['Almoços Diários', 'Planos Semanais', 'Opções Vegetarianas', 'Dietas Especiais']
  }
];
---

<Layout title="Catering & Serviços" description="Serviços de catering profissional Ayronia">
  <div class="min-h-screen bg-base-200">
    <div class="hero bg-primary text-primary-content py-20">
      <div class="hero-content text-center">
        <div class="max-w-md">
          <h1 class="text-5xl font-bold mb-4">Catering & Serviços</h1>
          <p class="text-xl">Transforme o seu evento numa experiência memorável</p>
        </div>
      </div>
    </div>

    <div class="container mx-auto px-4 py-16">
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-16">
        {services.map(service => (
          <div class="card bg-base-100 shadow-xl">
            <div class="card-body items-center text-center">
              <div class="text-6xl mb-4">{service.icon}</div>
              <h2 class="card-title text-2xl mb-3">{service.title}</h2>
              <p class="mb-4">{service.description}</p>
              <ul class="list-disc list-inside text-left w-full">
                {service.features.map(feature => (
                  <li class="mb-2">{feature}</li>
                ))}
              </ul>
            </div>
          </div>
        ))}
      </div>

      <div class="card bg-base-100 shadow-xl">
        <div class="card-body">
          <h2 class="card-title text-3xl mb-6 text-center">Solicitar Orçamento</h2>
          <form class="space-y-4" id="catering-form">
            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
              <div class="form-control">
                <label class="label">
                  <span class="label-text">Nome Completo</span>
                </label>
                <input type="text" placeholder="Seu nome" class="input input-bordered" required />
              </div>
              <div class="form-control">
                <label class="label">
                  <span class="label-text">Telefone</span>
                </label>
                <input type="tel" placeholder="+258 XX XXX XXXX" class="input input-bordered" required />
              </div>
            </div>
            <div class="form-control">
              <label class="label">
                <span class="label-text">Email</span>
              </label>
              <input type="email" placeholder="seu@email.com" class="input input-bordered" required />
            </div>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
              <div class="form-control">
                <label class="label">
                  <span class="label-text">Tipo de Evento</span>
                </label>
                <select class="select select-bordered" required>
                  <option disabled selected>Selecione...</option>
                  <option>Evento Corporativo</option>
                  <option>Festa Privada</option>
                  <option>Refeições Diárias</option>
                  <option>Outro</option>
                </select>
              </div>
              <div class="form-control">
                <label class="label">
                  <span class="label-text">Número de Pessoas</span>
                </label>
                <input type="number" placeholder="0" class="input input-bordered" required />
              </div>
            </div>
            <div class="form-control">
              <label class="label">
                <span class="label-text">Data do Evento</span>
              </label>
              <input type="date" class="input input-bordered" required />
            </div>
            <div class="form-control">
              <label class="label">
                <span class="label-text">Detalhes / Observações</span>
              </label>
              <textarea class="textarea textarea-bordered h-32" placeholder="Conte-nos mais sobre o seu evento..."></textarea>
            </div>
            <div class="form-control mt-6">
              <button type="submit" class="btn btn-primary btn-lg">Enviar Pedido</button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>

  <script>
    document.getElementById('catering-form')?.addEventListener('submit', (e) => {
      e.preventDefault();
      alert('Obrigado! O seu pedido foi recebido. Entraremos em contacto em breve.');
      (e.target as HTMLFormElement).reset();
    });
  </script>
</Layout>
